<div>
  <h1 class="font-bold text-4xl">Bookings</h1>
  <% @booking_histories.each do |booking_history| %>
    <% service = booking_history.service %>
    <div class="bg-white p-6 rounded-lg shadow-lg mb-4">
      <h2 class="text-2xl font-bold mb-2"><%= service.name %></h2>
      <p class="text-gray-700 mb-4"><%= number_to_currency(service.price) %></p>
    </div>
  <% end %>
</div>
