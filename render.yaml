databases:
  - name: pos_db
    databaseName: pos_db
    user: obimbo
    plan: free

services:
  - type: web
    name: pos
    runtime: ruby
    plan: free
    buildCommand: "./bin/render-build.sh"
    # preDeployCommand: "bundle exec rails db:migrate" # preDeployCommand only available on paid instance types
    startCommand: "bundle exec rails server"
    envVars:
      - key: postgres://obimbo:JcWZ97nyelbXGStUsY1sxiJbgO26FHwZ@dpg-cplbqpn109ks73dppksg-a/db_pos_s2ee
        fromDatabase:
          name: pos_db
          property: connectionString
      - key: a80ff83a27fb6d0afc2f4434a22222d9
        sync: false
      - key: WEB_CONCURRENCY
        value: 2 # sensible default